# TODO

- [x] add skill to skills/telegram-download-chat/SKILL.md — Describe all cli usage with examples. (done)
- [x] Move the Usage section above the Installation section in README.
- [x] Explain entity identifiers in the GUI: how to download my Saved Messages and a private group.
- [x] [#80] HTML/PDF export: inline formatting + reply threads. https://github.com/popstas/telegram-download-chat/issues/80
  - Scope: inline formatting (bold/italic/underline/strikethrough/code/links from `entities`) in **HTML and PDF**; thread headers + reply anchors in **HTML only**; TXT unchanged; on by default (no new flags). All changes in `core/render.py` + tests in `test_telegram_download_chat.py`.
  - [x] Add `format_entities(text, entities, dialect)` helper in `render.py`: map Telegram UTF-16 `offset`/`length` to Python indices, wrap overlapping spans segment-by-segment (well-formed nesting), emit html or ReportLab-pdf tags, sanitize hrefs (scheme allowlist drops `javascript:`); html keeps `\n`, pdf uses `<br/>`. Add `first_line(text, limit=60)` helper.
  - [x] HTML text: register Jinja `fmt_entities` filter, change `{{ msg.text | e }}` → `{{ msg.text | fmt_entities(msg.entities) }}`; carry raw `entities` through `_preprocess_messages`.
  - [x] PDF text: replace `_xml_escape(text)` body construction with `format_entities(text, entities, "pdf")`.
  - [x] Reply citation: cite the replied-to message's first line as an anchor `href="#msg-<parent_id>"` (parent must be in export, else fall back to existing `quote_text`); add `id="msg-<id>"` to each bubble.
  - [x] Thread headers (HTML only, `_preprocess_messages(..., with_threads=True)` from `render_html`): keep chronological order; compute each message's thread = reply-chain root (walk `reply_to_msg_id`, cycle-guarded); inject `--- name ---` header on thread change; name = first line of root msg, fallback `Thread #<id>`; standalone messages get no header. PDF stays unaffected.
  - [x] Tests: `format_entities` unit tests (both dialects, UTF-16 emoji offset, overlap, `javascript:` dropped); HTML integration (thread headers only on change, none for standalone, recurrence, bubble anchors, reply-anchor + fallback); PDF smoke run. Then `pytest`, `black`, `isort`.
- [x] Improve GUI: parse more structured info from the download log and surface it in the GUI — media download progress (current/total, per-file) and the date of the last downloaded message. Have the core/CLI emit structured progress events the GUI can consume instead of scraping raw log text.
- [x] Add Windows app auto-update: on startup check GitHub releases/latest, compare to the running version, and offer to download/install the new build. Model after `~/projects/python/talks-reducer/talks_reducer/gui/update_checker.py` (queries `releases/latest`, parses the version tag, compares versions, Windows-only).
- [x] After a `--media` download, show the count of downloaded media files and their total size.
- [x] html render: Don't cite messages that are thread roots.
- [x] e2e test doesn't download and render media.
- [x] pdf render: monospace font cyrillic is broken — register a Unicode mono font (DejaVu Sans Mono / Noto Sans Mono / Liberation Mono / Consolas) for code/pre spans instead of built-in Courier.
- [x] Получать названия threads (использовать имя топика форума, напр. "Formatting", вместо первой строки корневого сообщения). Проверять через e2e tests; один из топиков тестовой группы: Formatting.
- [x] HTML export: add topic tabs at the top of the page. Default tab "All" shows all messages as now. Each other tab is a forum topic (by topic name, e.g. "Formatting", "Topic 2") and filters to show only that topic's messages. Verify via e2e tests against the test group.
- [x] Add agent plugins/skills for Claude, Codex, and Cursor (package the telegram-download-chat CLI usage as installable skills that auto-activate per agent), modeled after superpowers (https://github.com/obra/superpowers). Reuse/extend the existing `skills/telegram-download-chat/SKILL.md`.
- [x] html render: в private чатах (и обычных группах) отвеченные сообщения отображаются как топики (`--- name ---` thread headers). Топики/тред-хедеры должны показываться только в супергруппах-форумах. Гейтить тред-хедеры (и подавление цитаты корня) на forum-детект (`MessageActionTopicCreate` или `reply_to.forum_topic`). Проверять через e2e.
- [x] html render: при переходе к сообщению по якорю (`#msg-<id>`) сообщение скрывалось за fixed шапкой — сделать шапку `static` (+ `scroll-margin-top` на бабблах под sticky-табами).
- [x] html render: при скачивании форума за период (напр. `--last-days 5`) топики ломались — тред-хедеры группировались по reply-цепочке, а не по реальному forum topic id (reply_to_top_id / forum_topic reply_to_msg_id), имена топиков были мусором, табов не было. Группировать по topic id; докачивать названия топиков при скачивании (`fetch_forum_topics` → `forum_topic_title`), фолбэк на первую строку in-window сообщения / `Thread #id`. Проверено на live windowed форуме.
- [x] GUI: подсказка для поля "Chat" (как скачать Saved Messages = `me`, приватный чат, группу, папку, конвертацию). Сворачиваемый блок "How to fill this?" (по умолчанию on) + опциональная иконка ⓘ с tooltip (по умолчанию off). Флаги в конфиге: `gui_chat_hint_tooltip` (off), `gui_chat_hint_help` (on). Цвет текста наследуется из темы (читается на тёмной/светлой).
- [ ] Download a channel together with its comments. A broadcast channel's comments live in a linked discussion supergroup — resolve that linked group yourself (e.g. via the channel's `linked_chat_id` / full-channel info) and download the per-post comment threads alongside the channel posts. Example: channel `@seeallochnaya`. Verify via e2e against a real channel-with-comments while implementing.
  - Scope: opt-in `--comments` flag (CLI + GUI checkbox). Channel-only; no-op with info log on non-broadcast entities. Comments are merged into the **same** `messages.json` (combined output), each comment normalized to point at its parent channel-post id so the existing TXT/HTML reply-thread + anchor logic nests them with **zero export changes**. Fetch strategy: **approach A** — per-post `iter_messages(channel, reply_to=post_id)` (Telethon resolves the linked group + id-mapping internally). Default off; no change to existing channel-download behavior.
  - [ ] `core/comments.py`: `get_linked_discussion(downloader, entity)` — run `GetFullChannelRequest`, return `linked_chat_id` (or `None`). `download_post_comments(downloader, channel_entity, post_ids, *, silent, stop_check) -> list[dict]` — for each post id, page `client.iter_messages(channel_entity, reply_to=post_id)`; stamp each comment: set `reply_to.reply_to_msg_id` **and** top-level `reply_to_msg_id` to the channel post id, add `comment_of=<post_id>`, preserve the native discussion id under `discussion_msg_id`. Handle `MsgIdInvalidError`/comments-disabled per post (skip, continue) and `FloodWaitError` (sleep-retry, mirror `download_chat`). Respect `_stop_requested` between posts.
  - [ ] Wire into the download flow: in `process_chat_download` (cli/commands.py), when `args.comments` and the resolved entity is a `broadcast` channel with a linked group, fetch comments for the downloaded post ids and append to the message list **before** save/dedup/render. If no linked group: info log, skip, posts still saved.
  - [ ] CLI: add `--comments` to `cli/arguments.py` (+ `comments: bool = False` in `CLIOptions`); help text notes channel-only.
  - [ ] GUI: add a "Download comments" `QCheckBox` in `gui/tabs/download_tab.py` modeled on `media_chk` — checkbox def (~369), settings load (~607), collect (~665), `cmd_args.append("--comments")` (~771), save/restore (~963/~993). Append `--comments` to the worker subprocess command when checked.
  - [ ] Progress: emit a structured `type: "comments"` event per post (posts done/total, comments so far) via `core/progress.py`; GUI worker surfaces it (parallel to the `media` event handling in `gui/worker.py`).
  - [ ] Tests: unit — parent-id normalization (comment fetched under post P has `reply_to_msg_id == P`, `comment_of == P`, native id preserved as `discussion_msg_id`); no-linked-group path skips cleanly; comments-disabled post skipped. Integration — combined list threads correctly through existing TXT/HTML ordering (comment nested under its post). Then `pytest`, `black`, `isort`.
  - [ ] e2e: run against a real channel-with-comments (`@seeallochnaya`) with `--comments` (+ `--html`), verify comments appear nested under posts in JSON and rendered output; copy results to `~/tmp/e2e-tdc`.
